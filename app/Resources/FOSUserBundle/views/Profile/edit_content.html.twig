{% trans_default_domain 'FOSUserBundle' %}
<div class="container edit_container">
<div class="col-md-offset-3 col-md-6">
{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-primary btn-registration" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
        <btn class="btn btn-danger btn-registration" type="submit"><a href="{{path('fos_user_change_password')}}">Modification Mot de Passe</a></btn>
    </div>
</div>
</div>
{{ form_end(form) }}
